import type { NextPage } from 'next'import {useRouter} from "next/router";const Plan: NextPage = () => {    const router = useRouter();    return (        <div>            Plan index page            <button onClick={()=>{router.push("/trend/plan/3")}}>기획</button>            <button onClick={()=>{router.push("/trend/plan/5")}}>기획</button>            <button onClick={()=>{router.push("/trend/plan/2")}}>기획</button>        </div>    )}export default Plan